import React from 'react';

class ShoppingCart extends React.Components {
  contructor(props);
  super(props);
  this.state({
    bedModel: '',
    amount: ''
  })
}

render() {
  return (
    <div>

    </div>
  )
}